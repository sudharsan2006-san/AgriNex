rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /preBookings/{booking} {
      allow read, write: if request.auth != null;
    }
    match /notifications/{notification} {
      allow read, write: if request.auth != null;
    }
  }
}
